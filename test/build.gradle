plugins {
    id "com.jfrog.bintray" version "1.6"
    id "maven-publish"
}

dependencies {
    compile "com.marklogic:ml-junit:2.6.0"
    compile "org.springframework.batch:spring-batch-test:3.+"
}


task sourcesJar(type: Jar, dependsOn: classes) {
    baseName = "marklogic-spring-batch-test"
    version = testVersion
    classifier = 'sources'
    from sourceSets.main.allSource
}

jar {
    baseName = "marklogic-spring-batch"
    version = testVersion
}